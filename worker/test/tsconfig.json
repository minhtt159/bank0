{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "types": ["@cloudflare/workers-types"]
  },
  // assets.test.ts runs in Node (wrangler test harness), not workerd — it has
  // its own tsconfig.node.json so the two runtimes' globals don't collide.
  "include": ["./proxy.test.ts", "./env.d.ts"]
}
